import _strings;

public val strings = object {
    // : Fun(String, Sequenceable[String]) => String
    public def join fun(separator: String, sequenceable: Sequenceable[String]) =>
        joinSequence(separator, sequenceable.toSequence());
    
    public val joinSequence = _strings.joinSequence;
};
