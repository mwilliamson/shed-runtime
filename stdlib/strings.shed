module strings;

members {
    join,
    joinSequence
}

import _strings;

// : Fun(String, Sequenceable[String]) => String
def join fun(separator: String, sequenceable: Sequenceable[String]) =>
    joinSequence(separator, sequenceable.toSequence());

val joinSequence = _strings.joinSequence;
