module json;

members::
    parseString,
    stringifyString

import _json;

// TODO: should return an option or similar to handle invalid inputs
def parseString fun(value: String) => _json.parseString(value);
def stringifyString fun(value: String) => _json.stringifyString(value);
